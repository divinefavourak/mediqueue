# Copy to .env and fill in. NEVER commit .env -- it holds real credentials.
#
#   cp .env.example .env
#
# Generate the passwords rather than inventing them:
#   openssl rand -base64 24

DB_USER=mediqueue
DB_PASSWORD=

# The first administrator account, created once on an empty database.
# Sign in and change this password immediately afterwards.
ADMIN_EMAIL=admin@your-health-centre.ng
ADMIN_PASSWORD=
